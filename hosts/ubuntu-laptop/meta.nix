{
  type = "home-manager";
  system = "x86_64-linux";
  # hostname is optional for standalone home-manager hosts
  # (rebuild.sh falls back to `home-manager switch` for these)
  hostname = "ubuntu-laptop";
  username = "braden";
}
